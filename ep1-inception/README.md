Episode 1 - Inception offers following:
1. Recap: allowing to write code using plain HTML, then using Vanilla JavaScript basics.
2. React-via-cdn: 
- Core and basics of React libraries
- Why to CDN links?
- Understanding how React and ReactDOM libraries's `createElement` & `createRoot` work?
- What `React.createElement` creates actually initially?
- Who interacts with Browser DOM to render HTML content using React?